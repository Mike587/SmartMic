# Pixi — installed environments are regenerated from pixi.toml + pixi.lock
# (both of which ARE tracked). Never commit the multi-GB env tree.
.pixi/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Claude Code
.claude/

# Data / acquisition outputs (kept on F: drive, never in the repo)
*.czi
*.czexp
*.czexp.backup
*.png
*.log
logs/
log/
output/
temp_*.py
